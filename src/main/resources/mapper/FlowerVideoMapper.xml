<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ldh.dao.FlowerVideoMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.ldh.entity.FlowerVideo">
        <id column="video_id" property="videoId" />
        <result column="url" property="url" />
        <result column="user_id" property="userId" />
        <result column="status" property="status" />
        <result column="like" property="like" />
        <result column="update_time" property="updateTime" />
        <result column="create_time" property="createTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        video_id AS videoId, url, user_id AS userId, status, like, update_time AS updateTime, create_time AS createTime
    </sql>

</mapper>
